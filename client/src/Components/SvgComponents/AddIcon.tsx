import React from 'react';
import { ReactComponent as Add } from '../../Assets/add.svg';
import './icons.scss';

const AddIcon = () => {
  return (

      <Add className="icon__addicon" />

  );
};

export default AddIcon;
